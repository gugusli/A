[phases.setup]
nixPkgs = ["php", "phpExtensions.pdo_pgsql", "phpExtensions.curl", "phpExtensions.mbstring"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php -S 0.0.0.0:$PORT index.php"
