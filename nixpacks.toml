[phases.setup]
nixPkgs = ["php81", "php81Extensions.pdo_pgsql", "php81Extensions.curl", "php81Extensions.mbstring"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php -S 0.0.0.0:$PORT index.php"
